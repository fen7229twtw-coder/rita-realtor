# ===== 不上傳的內容 =====

# 熊課課堂教材專案：屬於付費教材，且含講師本人照片與個資，不公開散布
realtor-ai-booking/

# 591 刊登管理系統：含 591 登入 session 與物件資料。
# netlify.toml 設定 publish = "."，根目錄任何進版控的檔案都會被發佈到公開官網，
# 所以這個資料夾必須整包排除，絕對不可解除。
rita-591-console/

# 物件競品戰情室：本機單人後台，含案件與競品資料（data/db.json）。
# 同樣受 netlify.toml publish = "." 影響，必須整包排除。
realtor-competitor-board/

# 中控後台：只在本機跑的管理台，會讀上面幾個私人專案的資料。
rita-hub/

# 本機 Node.js 免安裝執行環境（121MB，不該進版控）
.tools/

# Claude Code 本機設定與開發伺服器腳本
.claude/

# 本機去背工具頁（一次性使用，不屬於官網內容）
_bgremove.html

# 作業系統雜項
.DS_Store
Thumbs.db
desktop.ini

# 青埔社區資料庫：產生的資料檔與我的私人註記。
# 資料檔可以隨時用「更新青埔資料.cmd」重跑產生，沒必要進版控；
# my-notes.json 是屋主聯絡方式與帶看心得，受 publish = "." 影響絕對不可公開。
tools/qingpu-communities/data/

# 物調自動生成：純內部作業工具，且 template.docx 是有巢氏的表格範本。
# netlify.toml publish = "." 會把根目錄進版控的檔案發佈到公開官網，所以整包排除。
# 排除後仍可從 localhost:8899/tools/property-survey/ 與中控台正常使用。
tools/property-survey/

# 青埔地圖：只排除 Overpass 原始快取（2.5MB，隨時可用「更新青埔地圖.cmd」重抓）。
# 注意跟上面 qingpu-communities 的規則刻意不同：地圖的 data/*.json 要進版控。
# 這張圖是行銷物，netlify publish="." 發佈到官網、客戶點 QR 才看得到，
# 底圖是 OSM、行情是實價登錄，都是可公開資料。
tools/qingpu-map/data/cache/

# 自動更新的工作檔：狀態檔每跑一次就變、備份檔只是還原用的，
# 進版控只會把紀錄洗掉，而且對客戶端沒有意義。
tools/qingpu-map/data/auto-update-state.json
tools/qingpu-map/data/pins.backup.json
# 信義社區頁抓來的座標，是 build/refine-pins.mjs 的輸入不是成品。
# 跟 OSM 的 cache 同樣道理：別人網站的資料不要跟著發佈到公開官網。
# 沒有這個檔照樣能跑，只是少一個對照來源；要補就跑 build/fetch-sinyi-xy.mjs。
tools/qingpu-map/data/sinyi-xy.json
# 位置待確認清單：這是我自己校正時用的工作清單，不是給客戶看的東西。
# 校正是在本機（中控台 localhost:9000）做的，公開官網不需要這個檔。
tools/qingpu-map/data/pins-review.json
tools/qingpu-map/data/pins.bak-before-refine.json

# 開發信生成：裝的是整棟社區屋主的姓名與寄信住址，全都是個資。
# netlify.toml publish = "." 會把根目錄進版控的檔案發佈到公開官網，
# 這一包外流的後果比其他工具嚴重得多，整包排除，絕對不可解除。
# 排除後仍可從 localhost:9000/tools/dev-letter/ 與中控台正常使用。
tools/dev-letter/

# 物件追蹤：整頁靠中控台的 API 供資料，發佈到公開官網只會是一頁「連不上中控台」，
# 而且等於把內部工具結構攤在外面。清單本身存在 rita-hub/data/（已整包排除）。
# 排除後仍可從 localhost:9000/tools/watch-list/ 與中控台正常使用。
tools/watch-list/

# 帶看路線：你自己排路線用的內部工具，裡面會存客戶名與帶看備註（在 localStorage，
# 不會進檔案，但這頁本身也沒有對外的價值 —— 客戶拿到的是名片頁連結 card/#v=…，不是這頁）。
# netlify.toml 是 publish="."，不排除就會發佈到公開官網。
tools/tour-route/

# 青埔花媽Rita 官方 LINE 的客服回覆：整包是你的話術資產 —— 議價策略、擋殺價的說法、追蹤節奏，
# 同業拿去照抄的成本是零。netlify.toml publish="." 會把根目錄進版控的檔案
# 發佈到公開官網，所以整包排除。
# 排除後仍可從 localhost:9000/tools/line-reply/ 與中控台正常使用。
tools/line-reply/

# 離線單檔版是「更新青埔地圖.cmd」每次重跑就會重產的產物，不進版控。
# 它是拿來用 LINE 傳檔給客戶的，不是靠網址開的。
tools/qingpu-map/青埔地圖_離線版.html

# RITA 修圖系統：inbox/ 與 out/ 裝的是屋主家裡的照片，外流等同把客戶的家公開。
# netlify.toml publish="." 會把根目錄進版控的檔案發佈到公開官網，所以整包排除。
# 排除後仍可從 localhost:9000/tools/photo-studio/ 與中控台正常使用。
#
# ⚠ 這條要繼續留著，不要因為「想給同事用」就打開。
#   給同事的那一份走 p/ 那條路（見下面），是另外產出來的加密檔，
#   跟這個資料夾裡的照片、node_modules、.publish.json 完全分開。
tools/photo-studio/

# 各社區格局圖庫：圖是建商與代銷做的，而且每張都帶 591 浮水印。
# 自己帶看前查沒問題，公開重製就是另一回事 —— netlify.toml publish="." 會把根目錄
# 進版控的檔案發佈到公開官網，所以整包排除，跟修圖系統同一個理由。
# 排除後仍可從中控台與 localhost 正常使用。
# 資料檔（units.json、match.json）隨時可用 build/ 底下的腳本重跑產生。
tools/qingpu-layouts/

# RITA 室內設計系統：跟修圖系統同一個理由 —— netlify.toml publish="." 會把根目錄
# 進版控的檔案發佈到公開官網。這是她自己畫圖用的工具，沒必要公開，而且裡面
# 附了一份 687KB 的 three.js（3D 引擎），推上公開站等於白白多傳一份。
# 排除後仍可從 localhost:9000/tools/interior-studio/ 與中控台正常使用。
# 要公開的話把這一行拿掉就好，設計檔本身存在瀏覽器裡，不在版控中。
tools/interior-studio/

# 實價登錄異動通知：兩個理由都是硬的。
# ① data/line-config.json 裝的是 LINE 推播金鑰，netlify.toml publish="." 會把根目錄
#    進版控的檔案發佈到公開官網，金鑰外流等於任何人都能用你的機器人發訊息。
# ② data/cache/ 是政府那幾包 zip，一期 15MB、留六期就 90MB，不該進版控。
# 排除後仍可從 localhost:9000/tools/lvr-watch/ 與中控台正常使用。
tools/lvr-watch/

# 影片自動剪輯：inbox 是屋主家的影片、out 是剪好的成品，兩個都是客戶資產。
# netlify.toml publish="." 會把根目錄進版控的檔案發佈到公開官網，所以整包排除。
# 排除後仍可從 localhost:9200 與中控台正常使用。
tools/video-cut/inbox/
tools/video-cut/out/
tools/video-cut/work/
tools/video-cut/bgm/

# 時事貼文產生器：抓回來的是各報社的新聞標題與內文摘要，那是別人的著作，
# 不該由我的官網再發佈一次（netlify.toml publish="." 會把根目錄進版控的檔案
# 發到公開官網）。寫好的貼文與 Claude 金鑰存在 rita-hub/data/（已整包排除）。
# 排除後仍可從 localhost:9000/tools/news-post/ 與中控台正常使用。
tools/news-post/

# 591 青埔新案監看：兩個理由都是硬的。
# ① data/line-config.json 裝的是 LINE 推播金鑰，netlify.toml publish="." 會把根目錄
#    進版控的檔案發佈到公開官網，金鑰外流等於任何人都能用你的機器人發訊息。
# ② data/feed.json 是別人（同業與屋主）刊登的物件資料，抓回來自己看沒問題，
#    由我的官網再發佈一次就不對了。
# 排除後仍可從 localhost:9300 與中控台正常使用。
tools/591-watch/

# 屋主估價報告：三個理由。
# ① data/lvr/ 是切好的實價登錄（新北四年就 134MB），data/cache/ 是政府原始 CSV（71MB）。
#    netlify.toml publish="." 會把根目錄進版控的檔案發佈到公開官網，這種量級不該推上去，
#    而且隨時可以用「更新估價資料.cmd」重抓。
# ② 調整係數怎麼算（時間指數、樓層、坪數彈性、哪些訊號判定沒鑑別力）是估價方法本身，
#    跟 line-reply 的話術同一個道理，同業拿去照抄的成本是零。
# ③ 報告會印出屋主的門牌與謄本內容，工具頁本身沒有對外的價值。
# 排除後仍可從 localhost:9000/tools/valuation/ 與中控台正常使用。
tools/valuation/

# VIP 專屬市場報告：跟 valuation 同一個理由，而且更硬。
# ① 估價方法本身（濾掉預售簽約、行情指數怎麼建、可比社區怎麼挑、
#    樓層梯度什麼時候該不採用）是這一行的判斷力，同業拿去照抄的成本是零。
# ② POLICY_LIB 與新聞的「這對您的意思」是話術資產，跟 line-reply 同一個道理。
# ③ 後台頁面沒有對外價值 —— 客戶拿到的是 vip/<亂碼>/ 那一頁，不是這裡。
# 排除後仍可從 localhost:9000/tools/vip-report/ 與中控台正常使用。
tools/vip-report/

# ⚠ 上面排除 tools/vip-report/，但根目錄的 vip/ 刻意「不」排除 ——
# 那是要發佈給屋主看的成品，不進版控屋主的連結就打不開。
# 敢讓它公開的唯一理由：裡面的報告內容是 AES-256-GCM 鎖過的，
# 鑰匙只存在 rita-hub/data/vip-clients.json（rita-hub/ 整包已排除）
# 與屋主手上那條網址的 # 後面。# 後面的東西瀏覽器不會送給伺服器，也不會進 git。
# 所以就算有人翻這個 repo，看到的是一串亂碼。
#
# 🚫 vip/ 底下的檔案永遠不可以出現這兩樣的明文：屋主稱謂、社區名。
#    包含 <title>、OG 標籤、檔名、資料夾名。改 report.mjs 的時候要回來看這一條。

# 青埔在地監看：抓回來的新聞標題與摘要是別人的著作，
# 而且 netlify 是 publish="."，不排除的話會被發到公開官網。
# 排除後仍可從 localhost:9000/tools/qingpu-watch/ 與中控台正常使用。
tools/qingpu-watch/

# ── p/ ── 給同事用的加密發佈檔（要進版控，這是刻意的）
#
# p/<亂碼>/index.html 是各工具的 publish.mjs 產出來的單一檔案：
# 整包用 AES-256 鎖起來，密碼是解鎖的鑰匙。沒有密碼打開只有亂碼。
# 現在有兩支在用：
#   tools/photo-studio/publish.mjs      修圖系統（只鎖程式）
#   tools/property-survey/publish.mjs   物調系統（程式＋社區資料庫＋公司的物調表範本）
#
# ⚠ 物調那份鎖進去的東西比較敏感（有巢氏的表格範本、143 個社區的戶數與公設比），
#   所以密碼比修圖那份更要緊。網址不是秘密，密碼才是。
#
# ⚠ 這個資料夾只能放 publish.mjs 產出來的東西，不要手動丟任何檔案進去 ——
#   repo 是公開的，丟什麼進去全世界都看得到。
